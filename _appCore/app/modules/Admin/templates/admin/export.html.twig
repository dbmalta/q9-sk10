{% extends "layouts/admin.html.twig" %}

{% block title %}{{ t('nav.export') }}{% endblock %}

{% block content %}
<div class="container py-4">
    <h1 class="h3 mb-4">{{ t('nav.export') }}</h1>

    <div class="card">
        <div class="card-body">
            <p class="text-muted">{{ t('export.intro') }}</p>
            <a class="btn btn-primary" href="{{ route('admin.export.users_csv') }}">
                <i class="bi bi-download me-1"></i>{{ t('export.users_csv') }}
            </a>
        </div>
    </div>
</div>
{% endblock %}
