{% extends "layouts/base.html.twig" %}

{% block title %}{{ t('error.403.title') }}{% endblock %}

{% block body %}
<div class="d-flex align-items-center justify-content-center" style="min-height: 60vh;">
    <div class="text-center">
        <h1 class="display-1 fw-bold text-body-secondary">403</h1>
        <h2 class="mb-3">{{ t('error.403.title') }}</h2>
        <p class="text-body-secondary mb-4">{{ t('error.403.message') }}</p>
        <a href="/dashboard" class="btn btn-primary">
            <i class="bi bi-house me-1"></i>{{ t('nav.dashboard') }}
        </a>
    </div>
</div>
{% endblock %}
